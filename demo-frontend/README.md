# SAEP Demo Frontend

This is a tiny static page used as the visible change target for the sandboxed agent demo.

Open `index.html` in a browser to view it. The e2e demo asks the sandbox to change the headline text and accent color, then checks `safe-run diff` for those reviewable changes.
